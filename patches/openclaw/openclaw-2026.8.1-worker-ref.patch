--- a/dist/prepared-model-catalog-worker-UM-2vbW2.js
+++ b/dist/prepared-model-catalog-worker-UM-2vbW2.js
@@ -126,7 +126,6 @@
 				...params.input.input.env
 			}
 		});
-		active.unref();
 		active.on("message", (message) => {
 			const request = pending.get(message.requestId);
 			if (!request) return;
@@ -142,6 +141,7 @@
 				request.reject(error);
 				stop(error);
 			} else request.resolve(message);
+			if (pending.size === 0 && worker === active) active.unref();
 		});
 		active.once("error", (error) => stop(error instanceof Error ? error : new Error(String(error))));
 		active.once("exit", (code) => {
@@ -162,6 +162,7 @@
 			return Promise.reject(error instanceof Error ? error : new Error(String(error)));
 		}
 		const requestId = nextRequestId++;
+		active.ref();
 		return new Promise((resolve, reject) => {
 			const timeout = setTimeout(() => {
 				stop(/* @__PURE__ */ new Error("prepared model catalog worker timed out"));
@@ -172,10 +173,17 @@
 				resolve,
 				reject
 			});
-			active.postMessage({
-				...value,
-				requestId
-			}, []);
+			try {
+				active.postMessage({
+					...value,
+					requestId
+				}, []);
+			} catch (error) {
+				pending.delete(requestId);
+				clearTimeout(timeout);
+				if (pending.size === 0 && worker === active) active.unref();
+				reject(error);
+			}
 		}).then((message) => {
 			if (!params.isCurrent()) throw superseded();
 			return message;
